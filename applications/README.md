# Applications

One folder per application, keyed by the job `id` (e.g. `2026-001/`), holding the
tailored resume version and cover letter for that specific submission. The
sortable application metrics live in `data/applications.csv`.
