# Navigation for the in-app User Manual (/docs). Controls grouping + order only; each page's
# title comes from its first Markdown heading. Slugs map to <slug>.md in this folder.
sections:
  - title: Getting started
    pages:
      - install
      - what-is-peregrine
  - title: Do your first tasks
    pages:
      # cv-help before check-fit: fit scores are computed AGAINST the profile, so a
      # literal-order reader must build one before evaluating anything.
      - find-jobs
      - cv-help
      - evidence-library
      - check-fit
      - prepare-apply
      - track-applications
  - title: Going further
    pages:
      - target-scan
      - modes
  - title: Advanced
    pages:
      - edit-data
      - datasets
      - privacy-compliance
