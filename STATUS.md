# STATUS

> Auto-generated by the agent harness at runtime. A fresh session can read this
> file plus `logs/agent.log` to resume. This initial version is seeded by hand;
> the backend overwrites it once it boots.

- **Updated:** project scaffold
- **Current task:** idle (MVP scaffold complete)

## Todo
- [ ] Run `docker compose up --build` and smoke-test the chat + scan + apply gate
- [ ] Implement Ashby and Lever providers in `api/app/agent/providers.py`
- [ ] Add a "paste a URL" box in the web UI (chat already handles pasted URLs)
- [ ] Wire `applications.csv` updates when a user marks a job as applied
- [ ] Add cover-letter generation under `materials-prep` + `templates/`
- [ ] Add a derived SQLite index for fast search over many jobs
- [ ] Add tests for `data_store` and providers

## Recent activity
- feat: amazon.jobs provider + `ingest_job_url` tool + `POST /api/jobs/ingest`
  (paste a job URL in chat -> ingest -> evaluate). Validated against a real
  Amazon posting (id 3196773): correct parse, full snapshot, dedup on
  company+company_job_id.
- scaffold: backend (FastAPI agent harness, tools, subagents, providers, data store)
- scaffold: frontend (React chat + jobs table + job detail with apply gate)
- scaffold: skills, config templates, docker-compose, docs
